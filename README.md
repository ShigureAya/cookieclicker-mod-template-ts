# cookieclicker-mod-template-ts

Template project for developing a CookieClicker mod.

## Build Setup

Change package.json

```json
"modName": "SampleMod"//Change to your mod name
```

``` bash
# install dependencies
npm install

# build
npm run build

# Detects file changes and builds them
npm run watch
```
